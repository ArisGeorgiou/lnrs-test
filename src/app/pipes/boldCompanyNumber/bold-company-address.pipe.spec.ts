import { BoldCompanyAddressPipe } from './bold-company-address.pipe';

describe('BoldCompanyAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldCompanyAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
